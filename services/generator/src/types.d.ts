export type SocketEvent = {
    body: any,
    senderId: string
}

export type Restaurant = {
    id: string,
    restaurantName: string,
    description: string,
    rating: string,
    price: string,
    backgroundImage: string
  }